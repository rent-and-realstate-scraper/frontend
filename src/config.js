
const config = {
    backendUrl: process.env["BACKEND_URL"] || 'localhost:3000',
};
/*
const config = {
    backendUrl: process.env["BACKEND_URL"] || 'https://hbello.info',
};
*/
export default config;
